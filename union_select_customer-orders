--SELECT KUNDENID, COALESCE(SUM(PREIS), 0) FROM(
SELECT KUNDENID,
  SUM(PREIS)AS TOTAL
FROM BESTELLUNGEN GROUP BY KUNDENID
UNION ALL
SELECT KUNDENID,
  0 AS PREIS
FROM KUNDEN
WHERE KUNDENID NOT IN
  (SELECT KUNDENID FROM BESTELLUNGEN
  ) GROUP BY KUNDENID;
